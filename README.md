# Sistema de Alumnos

Practica de POO en C# (.NET 10).

Clases: `Persona` (base), `Alumno` y `Profesor` (heredan de `Persona`), `Preceptor`,
`Materia` (no hereda) y la interfaz `IExportable`.

Para ejecutarlo:

    dotnet run --project SistemaAlumnos
